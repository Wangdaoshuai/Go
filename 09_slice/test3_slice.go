package main

/*
import "fmt"

//声明切片的方式
func main() {
	//声明slice是一个切片，并且初始化，默认值1，2，3  长度len为3
	slice1 := []int{1, 2, 3}

	//声明slice2是一个切片，但并没有给slice分配空间
	var slice2 []int
	slice2 = make([]int, 3) //开辟空间，默认值为0
	slice2[0] = 100
	//声明slice3是一个切片，同时给slice分配空间
	var slice3 []int = make([]int, 3)
	//声明slice4是一个切片，同时给slice分配空间,通过：=推导出slice是一个切片
	slice4 := make([]int, 3)

	fmt.Printf("len = %d, slice = %v\n", len(slice1), slice1) //%v 打印详细信息
	fmt.Printf("len = %d, slice = %v\n", len(slice2), slice2) //%v 打印详细信息
	fmt.Printf("len = %d, slice = %v\n", len(slice3), slice3) //%v 打印详细信息
	fmt.Printf("len = %d, slice = %v\n", len(slice4), slice4) //%v 打印详细信息

	//判断一个slice是否为0
	if slice1 == nil {
		fmt.Println("slice1是一个空切片")
	} else {
		fmt.Println("slice1是有空间的")
	}
}
*/
